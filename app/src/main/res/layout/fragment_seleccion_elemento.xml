<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable name="viewModel" type="es.uam.eps.dadm.santioscar.renovium.IntroGameViewModel"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@id/tvElementosLeft"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Texto introductorio -->
        <TextView
            android:id="@+id/gameIntroText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/intro_text"
            android:textSize="18sp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:gravity="center"
            android:layout_marginBottom="20dp"/>

        <!-- Imagen del elemento -->
        <ImageView
            android:id="@+id/elementoImage"
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:layout_constraintTop_toBottomOf="@id/gameIntroText"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="20dp"/>

        <!-- Botones de navegación -->
        <Button
            android:id="@+id/elementoPrevButton"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:text="←"
            app:layout_constraintEnd_toStartOf="@id/elementoImage"
            app:layout_constraintTop_toTopOf="@id/elementoImage"/>

        <Button
            android:id="@+id/elementoNextButton"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:text="→"
            app:layout_constraintStart_toEndOf="@id/elementoImage"
            app:layout_constraintTop_toTopOf="@id/elementoImage"/>

        <!-- Etiqueta -->
        <TextView
            android:id="@+id/selectElementoLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/elementoImage"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="10dp"
            android:gravity="center"/>

        <!-- Texto de elementos restantes -->
        <TextView
            android:id="@+id/tvElementosLeft"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/selectElementoLabel"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="27dp"/>



    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>